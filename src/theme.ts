import {DefaultTheme} from 'styled-components';

export const theme : DefaultTheme = {
    bgColor: "#2f3640",
    textColor: "#f5f6fa",
    accentColor: "#4cd137",
};

export const darkTeme : DefaultTheme = {
    bgColor : "black",
    textColor : "white",
    btnColor : "teal"
}